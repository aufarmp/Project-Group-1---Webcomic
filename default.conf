server {
    listen 80; # Biasanya Dockerfile nginx expose 80, mapping di compose yang 85:85
    root /var/www/myphpapp; # Sesuai volume di docker-compose
    index login.php index.php;

    access_log /var/log/nginx/myphpapp_access.log;
    error_log /var/log/nginx/myphpapp_error.log;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass php:9000; # Mengarah ke service PHP
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
    }
}